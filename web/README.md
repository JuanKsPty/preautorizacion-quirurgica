# Frontend

React + Vite + TypeScript. Se arranca desde la raiz del proyecto:

```bash
pnpm dev          # frontend y API juntos
pnpm run dev:web  # solo el frontend
```

Documentacion, comandos y convenciones: [`../README.md`](../README.md) y
[`../CLAUDE.md`](../CLAUDE.md).
